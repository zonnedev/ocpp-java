final class FormatterExample {
  void method(
    String someParam,
    int someOtherParam,
    String moreParam
  ) {
    SomeClass.builder()
      .param1("something")
      .param2("more")
      .build();

    stream.map(it -> it * 2);
    stream.map(it -> {
      // some stuff
      return it * 2;
    })
      .map(it -> it * 3);
  }
}
